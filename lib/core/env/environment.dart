/// The `Environment` class holds configuration constants for the application.
final class Environment {
  /// The `api` constant key represents the API endpoint configuration
  /// and can be used to query environment variables. Example:
  /// ```dart
  /// final api = String.fromEnvironment(Environment.api);
  /// ```
  static const api = 'API';
}